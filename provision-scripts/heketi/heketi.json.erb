
{
  "clusters" : [
    {
      "nodes" : [
        <% (1..numWorkers).each do |i| %>
            {
            "node": {
                "hostnames": {
                    "manage": [
                        "<%=networkPrefix%>.<%=i + 100%>"
                    ],
                    "storage": [
                        "<%=networkPrefix%>.<%=i + 100%>"
                    ]
                },
                "zone": 1
            },
            "devices": [
                "/dev/sdc"
            ]
        }<%if (i != numWorkers)%>,<%end%>
        <% end %>
      ]
    }
  ]
}
EOF
